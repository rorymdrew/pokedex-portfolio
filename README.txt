brock.png — trainer-card portrait (Brock's HG/SS VS sprite, 96x64, shown
pixel-crisp in the gold frame).

To swap the portrait, replace brock.png with any image (and keep the filename,
or update the <img src> in index.html). If the file is missing, the site shows
a pixel-art placeholder instead.
